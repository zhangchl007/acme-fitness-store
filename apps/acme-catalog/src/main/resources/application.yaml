spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/acmefit?useSSL=false&currentSchema=public
    username: "root"
    password: "rootpassword"
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
eureka:
  client:
    enabled: false
